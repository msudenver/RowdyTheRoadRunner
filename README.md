####1. Install dependencies with:

``` # sudo npm install ```

``` # bower i ```

####3. Prepares preview and prod directories:

``` # gulp prep ```

####3. Run with livereload workflow:

``` # gulp serve ```
